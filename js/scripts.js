
//Business Logic

function beepBoop(number) {
    const numInput = parseInt(number);
    const emptyArray = [];
    for (let i = 1; i <= numInput; i++) {
        emptyArray.push(i);
}
return emptyArray;
}


beepBoop("12");


console.log(index);


const emptyArray = [];
const answerArray = array.map(function(num) {
    return num
});
answerArray;
    return all numbers from 1 to the number they chose
}